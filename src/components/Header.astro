---
interface Props { current?: 'projects' | 'blog' }
const { current } = Astro.props;
---
<header class="mx-auto max-w-5xl px-4 py-6 flex items-center justify-between">
  <a href="/" class="font-semibold text-lg">Adrián Mariscal</a>
  <nav class="flex gap-5 text-sm">
    <a href="/projects" class={`hover:underline ${current==='projects' ? 'font-semibold' : ''}`}>Proyectos</a>
    <a href="/blog" class={`hover:underline ${current==='blog' ? 'font-semibold' : ''}`}>Blog</a>
    <a href="mailto:tuemail@dominio.com" class="hover:underline">Contacto</a>
  </nav>
</header>
