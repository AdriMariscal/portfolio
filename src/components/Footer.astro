---
/* Si TypeScript protesta por el import JSON, añade "resolveJsonModule": true en tsconfig.json
   o pon // @ts-ignore en la línea del import. */
import pkg from '../../package.json' assert { type: 'json' };
const version = pkg.version as string;
const year = new Date().getFullYear();
---
<footer class="mt-16 border-t py-8 text-sm text-center text-gray-500">
  <p>© {year} Adrián Mariscal · v{version}</p>
</footer>
